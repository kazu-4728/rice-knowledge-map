# CLAUDE.md

@AGENTS.md

## Claude Code 固有ルール

- 作業開始時は、まず `tasks/TASKS.md` の「現在の実行タスク」を読む。
- 長時間のUI改修でも、現在タスクで定義された利用体験が完成するまで同じブランチ・同じDraft PRで進める。
- Draft解除は、受入条件を満たし、セルフレビュー完了後に行う。
- PR完了報告には、対応内容、修正コミット、検証結果、未確認事項を日本語で記載する。
- レビュー指摘を修正した場合は、該当スレッドへ返信し、解決済みならResolveする。
- squashマージ後は `tasks/TASKS.md` の現在タスク・次タスク・ユーザー確認待ちだけを更新する。

## セッション記録の保存ルール

通常の開発フロー（コミット・コミットメッセージ・PR作成など）はこれまで通り
リポジトリ内で行うこと。このルールはそれとは別に、**Claude Codeとの
チャットセッションの内容（何を考え、何を話したか）**をGoogleドライブに
保存するためのものであり、リポジトリの成果物には影響しない。

手順（セッションの区切り、または作業がまとまった時点で実行）:

1. Google Driveの `create_file` ツールを使い、以下のフォルダに保存する。
   - parentId: `1Lby7XlZ9QPTQZpkw-3Zu0St-wk13mTrx`
2. ファイル名: `YYYY-MM-DD-HHmm-<内容を表す短いタイトル>.md`
3. 内容は以下の構成で書くこと（チャットの生ログではなく要約でよい）:
   - `## 何を話し、何を判断したか`（セッション中の要点）
   - `## なぜそう判断したか`（理由・迷った点・却下した案）
   - `## 関連する変更`（このセッションでコミットしたものへの参照。内容自体はリポジトリ側にあるので繰り返さない）
   - `## 次にやること / 未解決の点`
4. **このファイルおよびセッション記録は git add / commit しない**こと。
   コミット・PRは通常通りリポジトリで完結させる。

（正本は `claude-harness/templates/dispatch-session-log.md`）
