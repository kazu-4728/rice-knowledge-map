# TASKS.md

本番URL: https://rice-knowledge-map.vercel.app

## 読み方

- 状態は `TODO / IN_PROGRESS / DONE` のみ。
- 完了の証拠は本ファイルではなくGitHub PR（番号・マージコミット）を参照する。過去の実装詳細・作業ログ・旧UI方針はここには残さない。
- 「現在の実行タスク」が唯一の正。他のMarkdownに矛盾する記述があっても、ここを優先する。
- 新しいセッションはまず「現在の実行タスク」だけを読めば作業を再開できる状態を保つ。

## 現在の実行タスク

**実機一周確認: 主要導線の残存混乱を洗い出す（Issue #62）**

- 状態: TODO
- 背景: Issue #58「入口・画像確認・今日の田んぼ導線の混乱解消」はPR #59でmainへ反映・マージ済み（Issue #58はクローズ済み）。Issue #58コメントの完成ロードマップの次のステップとして、実機で主要導線を一周し、残っている混乱や小さな不具合を洗い出す。
- 目的: `/` → ログイン → `/home` → `/map` → `/talk` → `/records` → `/fields` → `/menu/site` を iPhone Safari / PWA / PCブラウザで一周し、導線の混乱・表示崩れ・言葉遣いの違和感を一覧化する。大きな再設計はせず、次に着手すべき小さな改善点を洗い出す。
- 今回の対応範囲: 実機一周確認の実施と発見した問題の一覧化。明らかに小さく安全な修正（文言・レイアウト崩れ等）はこのIssueの中で直す。大きめの改修は次Issueとして切り出す提案をする。
- 非対象: 大規模な再設計・新規ルート追加（`/start` `/manage` `/app`）、MapCanvas内部ロジック、DBスキーマ変更。
- PR単位: Issue #62のスコープを1つのPRにまとめる。実機確認結果と発見した問題の一覧をPR本文に明記する。
- 詳細はIssue #62を参照。

## 次の実行候補

- 主要タスクの完了体験磨き（記録作成・田んぼ確認・写真比較・家族コメント・未対応解消）
- `/manage`へのURL統一（`/home`のリネーム。Issue #58の将来候補として保留）
- PWA再起動時に前回作業画面へ戻すゲートウェイ設計（`/app`案はIssue #58の将来候補として保留）
- PC対応（モバイル優先方針のため後回しにしてきたが、田んぼOS一連の改修が落ち着き次第着手する）
- T-048: 記録のAI整理・要約（任意機能）
- T-052: コメントの編集・削除（長押し/スワイプ等の操作導線を再検討）
- T-053: 複数グループの本格対応
- T-054: 記録のsoft-delete（`deleted_at` + RLS）
- U-004: Supabaseレガシーanonキーの無効化（任意・ユーザー作業）

## ユーザー確認待ち

- `/start`、`/manage`、`/app` の新設を将来PRで検討するかの判断
- PC対応に着手してよいタイミングの判断
