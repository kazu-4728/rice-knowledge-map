# TASKS.md

本番URL: https://rice-knowledge-map.vercel.app

## 読み方

- 状態は `TODO / IN_PROGRESS / DONE` のみ。
- 完了の証拠は本ファイルではなくGitHub PR（番号・マージコミット）を参照する。過去の実装詳細・作業ログ・旧UI方針はここには残さない。
- 「現在の実行タスク」が唯一の正。他のMarkdownに矛盾する記述があっても、ここを優先する。
- 新しいセッションはまず「現在の実行タスク」だけを読めば作業を再開できる状態を保つ。

## 現在の実行タスク

**次の実行タスク未定（田んぼOS UI/UX全面作り直し、PR #57マージ済み）**

- 状態: TODO（範囲確定前。オーナー判断待ち）
- 前提: 以前ここに記載していた「主要導線の実態確認」は、オーナー指示（「マップ以外は破壊して作り直す。データ層も含めて再構成してよい」）により大規模改修へ拡大し、PR #57としてマージ済み。対応内容:
  - データ層再構成: `fieldAttention.ts`で重複集計を一本化、`fields/page.tsx`・`FieldDetailScreen.tsx`のSupabase直呼びを`lib/data/farm.ts`へ集約、画面別カスタムフック導入
  - 全画面（`/home`, `/talk`, `/records`, `/fields`, `/fields/[id]`, `/calendar`, `/menu`）に固有ヒーローを導入
  - 全画面の主役ビジュアルをSVGモック/抽象図形から実写へ置き換え。オーナー提供の生成画像5枚をSupabase Storage公開バケット`app-defaults`で管理し、差し替え可能スロット（`group_site_content.image_slots`）を`/menu/site`に追加
  - `TodayStory`を`/home`にも統合（従来は`/map`のみ）。背景は実写優先（最新記録実写→差し替え画像→既定実写→SVGの順）
- 未解決のまま残っている事実（前回記載分、今回のPRでは対象外のため未着手）:
  1. `/map`のガイドUX「次にやること」カード（`MapSummarySheet.tsx`）が初期状態で折りたたまれており、初見で見えない
  2. ダーク管制室マップはUIクローム（検索ピル・凡例等）のみダーク化されており、地図タイル本体はダーク化対象外
- 到達条件: 次の対象範囲（上記未解決2点を含む）について、オーナーが実機/本番で確認のうえ対応要否を判断すること。
- 範囲: 未定（オーナー判断待ち）
- 非対象: 現時点で未定。
- PR単位: 範囲確定後に決定。

## 次の実行候補

- `/map`の折りたたみUX・ダークマップの地図タイル化（未解決事実1・2、対応要否含め判断待ち）
- 一時作成したSupabase Edge Function`seed-default-images`の削除（既定画像取り込み用の使い捨て、不要なら削除可）
- PC対応（モバイル優先方針のため後回しにしてきたが、田んぼOS一連の改修が落ち着き次第着手する）
- T-048: 記録のAI整理・要約（任意機能）
- T-052: コメントの編集・削除（長押し/スワイプ等の操作導線を再検討）
- T-053: 複数グループの本格対応
- T-054: 記録のsoft-delete（`deleted_at` + RLS）
- U-004: Supabaseレガシーanonキーの無効化（任意・ユーザー作業）

## ユーザー確認待ち

- PR #57の実機確認: 実データ環境での写真アップロード動作、屋外での`glass-light`視認性、FABのタップ/長押し閾値（Vercelプレビューでの実写反映は確認済み）
- `/map`の折りたたみUX・ダークマップタイルの扱い（修正要否の判断、次タスクの範囲を左右する）
- PC対応に着手してよいタイミングの判断
