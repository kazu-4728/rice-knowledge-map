# TASKS.md

本番URL: https://rice-knowledge-map.vercel.app

## 読み方

- 状態は `TODO / IN_PROGRESS / DONE` のみ。
- 完了の証拠は本ファイルではなくGitHub PR（番号・マージコミット）を参照する。過去の実装詳細・作業ログ・旧UI方針はここには残さない。
- 「現在の実行タスク」が唯一の正。他のMarkdownに矛盾する記述があっても、ここを優先する。
- 新しいセッションはまず「現在の実行タスク」だけを読めば作業を再開できる状態を保つ。

## 現在の実行タスク

**田んぼOS: 主要導線の実態確認と次の完成体験の定義**

- 状態: TODO（実装着手前。範囲確定にオーナー判断が必要）
- 前提の訂正: 以前はPR-1〜3（PR #53/#54/#55）で「田んぼOS」の主要要素が完成した前提で次PRを一律「仕上げ」としていたが、mainの実コードを確認した結果、一部が主要導線として機能していないことが判明した。次PRの範囲はこの事実を踏まえて確定する。
- 確認済みの事実（2026-07-05, main HEAD時点でコードを直接確認）:
  1. `/map`のガイドUX「次にやること」カード（`src/features/map/MapSummarySheet.tsx`）は下部シートが初期状態で折りたたまれているため、画面初見では表示されない（展開操作をして初めて見える）。
  2. 「今日の田んぼ」ストーリー（`src/features/story/TodayStory.tsx`）は`/map`（`MapScreen.tsx`）にのみ組み込まれており、`/home`には組み込まれていない。
  3. 「ダーク管制室マップ」は検索ピル・凡例・コントロール・サマリーシート・FABメニューなどUIチロームのみがダーク/ガラス化されており、地図本体（国土地理院空中写真タイル）はダーク化の対象外。
  4. 一方、ナビ4系統化、`/talk`統合トークルーム（複数導線あり）、農事暦タイムライン（`/home`ファーストビューに表示）、田んぼ詳細「定点観測」タブ、`/fields`・`/records`一覧の大判カード化は、主要導線として機能していることを確認済み。
- 到達条件: 上記1〜3について、オーナーが「意図した見え方である」または「主要導線として機能するよう修正が必要」と判断できる状態にする。
- 範囲: 次の実装PRの範囲は、上記1〜3に対するオーナーの判断を踏まえて確定する。確定後は小修正の積み重ねではなく、対象箇所を1つの完成体験としてまとめて実装する。
- 非対象: MapCanvas内部（描画・ピン・面積計算・保存ロジック）の変更、DBスキーマ変更。
- PR単位: 範囲確定後の実装は1つのPRにまとめる。

## 次の実行候補

- PC対応（モバイル優先方針のため後回しにしてきたが、田んぼOS一連の改修が落ち着き次第着手する）
- T-048: 記録のAI整理・要約（任意機能）
- T-052: コメントの編集・削除（長押し/スワイプ等の操作導線を再検討）
- T-053: 複数グループの本格対応
- T-054: 記録のsoft-delete（`deleted_at` + RLS）
- U-004: Supabaseレガシーanonキーの無効化（任意・ユーザー作業）

## ユーザー確認待ち

- 上記「確認済みの事実」1〜3が意図した状態か、修正が必要かの判断（次PRの範囲を左右する）
- 田んぼOS PR-3（管理空間+農事暦+定点観測、PR #55）の実機確認
- PC対応に着手してよいタイミングの判断
