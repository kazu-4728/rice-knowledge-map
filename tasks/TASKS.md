# TASKS.md

本番URL: https://rice-knowledge-map.vercel.app

## 読み方

- 状態は `TODO / IN_PROGRESS / DONE` のみ。
- 完了の証拠は本ファイルではなくGitHub PR（番号・マージコミット）を参照する。過去の実装詳細・作業ログ・旧UI方針はここには残さない。
- 「現在の実行タスク」が唯一の正。他のMarkdownに矛盾する記述があっても、ここを優先する。
- 新しいセッションはまず「現在の実行タスク」だけを読めば作業を再開できる状態を保つ。

## 現在の実行タスク

**田んぼOS: 入口・画像確認・ストーリー導線の混乱解消**

- 状態: TODO（Claude Code再開後、Issue #58を起点に実装前監査を行う）
- 背景: PR #57で全画面の実写優先化、`image_slots`、`app-defaults`、`TodayStory`の`/home`統合、主要画面ヒーロー刷新はmainへ反映済み。PR #57後の実機確認で、見た目の品質よりも「どこから始めるか」「編集した画像をどこで確認するか」「今日の田んぼストーリーをどこから見るか」が混乱していることが判明した。
- 重要な前提:
  1. GPT/Codexが作成したIssue #58は問題提起として参照するが、そのまま全面実装しない。
  2. `/start`、`/manage`、`/app` の新設や大規模なルート再設計は、今回の第一PRでは非対象とする。
  3. Supabase schema / RLS / Storage bucket 変更は今回の非対象。既存の `hero_slides`、`image_slots`、`app-defaults` を使う。
  4. 実装前に現在のルート、ログイン後遷移、`TodayStory`の表示条件、`/menu/site`の画像確認導線を監査し、PR本文に監査結果を書く。
- 確認済みの現状（2026-07-09, main HEAD時点）:
  1. `/` はランディングページだが、ログイン済みユーザーは `LandingScreen` で `/home` へ `replace` されるため、ランディング画像を編集後に確認しづらい。
  2. `/home` はナビ表示上「管理」だが、URL名は `home` のまま。実機ではホーム/管理/開始画面の役割が混ざって見える。
  3. `TodayStory` は `/home` と `/map` に組み込まれているが、1日1回表示・`?story=1`強制表示・閉じるとマップへ進む挙動がUI上分かりにくい。
  4. `hero_slides`（ランディング用）、`image_slots`（各画面カバー用）、`app-defaults`（システム既定画像）の役割が実装上は分かれているが、設定画面から「どこで確認できるか」が分かりにくい。
- 次PRの目的: 大規模再設計ではなく、ログイン後でもランディング画像と今日の田んぼストーリーを確認でき、`/home`が管理画面であることが分かる最小導線を整える。
- 想定スコープ:
  - ログイン済みでもランディングページを確認できる導線を用意する。
  - `/menu/site` から「ランディングを確認」「今日の田んぼを確認」へ移動できるようにする。
  - `TodayStory` の強制表示後に不要なクエリが残らないようにする。
  - `/home` が「管理」画面であることを見出し・説明・導線上で明確にする。
- 非対象: MapCanvas内部（描画・ピン・面積計算・保存ロジック）、DBスキーマ変更、Storage設定変更、PWA復帰ゲートウェイの本格実装、複数グループ対応、PC最適化の本格改修。
- PR単位: Issue #58の縮小スコープを1つのPRにまとめる。実装前監査、対応内容、非対象、実機確認結果をPR本文に明記する。

## 次の実行候補

- PC対応（モバイル優先方針のため後回しにしてきたが、田んぼOS一連の改修が落ち着き次第着手する）
- PWA再起動時に前回作業画面へ戻すゲートウェイ設計（`/app`案はIssue #58の将来候補として保留）
- T-048: 記録のAI整理・要約（任意機能）
- T-052: コメントの編集・削除（長押し/スワイプ等の操作導線を再検討）
- T-053: 複数グループの本格対応
- T-054: 記録のsoft-delete（`deleted_at` + RLS）
- U-004: Supabaseレガシーanonキーの無効化（任意・ユーザー作業）

## ユーザー確認待ち

- Issue #58の縮小後スコープでClaude Codeに着手させる判断
- `/start`、`/manage`、`/app` の新設を将来PRで検討するかの判断
- PC対応に着手してよいタイミングの判断
