# WORK_LOG.md

## 2026-05-30 UI Preview 0.1 作業ログ

### 実施内容

- Phase 1 UI Preview 用に、スマホ縦画面のダミーUIを実装した。
- `src/app/page.tsx` をPreview入口にし、画面・UI部品・ダミーデータを機能別に分割した。
- ホーム、マップ、記録、メニューの4タブ構成を追加した。
- マップ画面では国土地理院の空中写真タイルを `maplibre-gl` で表示し、読み込めない場合はCSS fallbackを表示する構成にした。
- 田んぼポリゴン、入水口、出水口、異常箇所ピン、下部詳細シートをダミーデータで表示した。
- 参照画像に寄せるため、Meiryo優先フォント、カード、チップ、下部タブ、マップUI、リストUIのCSSを整理した。

### 確認内容

- `npm run lint` 成功。
- `http://localhost:3002/` をスマホ幅でスクリーンショット確認した。
- `npm run build` は、稼働中のNext.js dev serverと同じ `.next` を更新して表示エラーを起こす可能性があるため、この時点では未実行。

### 未解決事項

- ユーザー確認では、参照モック画像の雰囲気・完成度にはまだ到達していない。
- 現在のCSS生成サムネイルと地図上の疑似表現では、実画像ベースの高品質な質感が不足している。
- 次回は、参照画像に近い実写真/地図素材の扱い、または既存UIコンポーネント/デザインシステム導入方針を再検討する。
- UIタスクは `DONE` ではなく、スクリーンショット確認済みのレビュー継続状態として扱う。

### Git管理メモ

- 参照画像はリポジトリへ追加していない。
- `.next/`、`node_modules/`、`tmp/`、`.npm-cache/` は `.gitignore` 対象。
- 作業中に生成した `tmp/` と `.npm-cache/` はコミット前に削除済み。
