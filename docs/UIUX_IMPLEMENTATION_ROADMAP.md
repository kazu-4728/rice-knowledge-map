# UI/UX 実装ロードマップ

最終更新: 2026-06-26

## 0. この文書の目的

この文書は、実画像マップ型 稲作ナレッジ記録アプリを、現場で使えるMVPへ段階的に引き上げるための実装順序です。

現在は「マップ中心の基本操作が形になり、実機確認フェーズへ入った」段階です。実機確認で見つかった導線課題を最小単位で改善することを最優先とします。

### 守ること

- 1 PR = 1目的。UI改修、構造整理、DB変更を混ぜない。
- 既存の実機確認済みフローを壊さない。
- UIを先に決め、DBや新機能を増やす前に操作体験を固める。
- 完了は「ビルド成功」だけでは不可。対象フローのPreview確認と、必要なiPhone実機確認を証拠に残す。
- 迷ったら「写真を撮る → 話す → 保存する → 実画像マップに残る」が短くなる方を選ぶ。

### 完了状態

| 状態 | 意味 |
|---|---|
| TODO | 未着手 |
| IN_PROGRESS | 実装または実機確認中 |
| DONE | PRマージ済み。必要な実機確認の証拠あり |
| DEFERRED | MVPでは後回し。再開条件を明記 |

---

## 1. 現在の基準線

### 完了済み

- マップを中心にした田んぼ選択、場所合わせ（2段階）、輪郭描画、名前入力、保存
- 1本指描画時のiOSブラウザジェスチャー漏れ防止
- iOS Safari 入力フォーカスによるビューポート拡大修正（input 16px化・blur-before-action）
- 田んぼ・地点ピン・記録・写真・音声・コメント・対応済み管理の基本機能
- モバイルDrawer / PC SideNav の基本ナビゲーション
- 田んぼ選択ホイールピッカー（FieldSearchSheet）

### 現時点で壊してはいけない代表フロー

1. `/map` → 田んぼを登録する → 場所を合わせる（placing）→ 輪郭を描く（drawing）→ 名前入力 → 保存 → 通常閲覧へ戻る
2. `/map` → 田んぼを選ぶ → ホイールを動かす（プレビュー）→ 正式選択 → 詳細を見る
3. 田んぼ詳細 → ピン追加 → 保存
4. 写真・音声を含む記録作成 → 保存前確認 → 保存 → 詳細表示
5. 記録詳細 → コメント → 対応済み

---

## 2. 実装順序

## Phase 1: 初回利用とマップ中心導線を完成させる

ユーザーの実機確認で見つかった課題を優先的に解消するフェーズ。

| ID | 状態 | 目的 | 実装範囲 | 完了条件 |
|---|---|---|---|---|
| UX-070 | TODO | 初回利用導線を決める | 未ログイン、ログイン済み・田んぼ0件、田んぼ登録済みの3状態を整理。最初に押すボタンを各状態で1つにする | 初回ユーザーが説明を読まなくても最初の田んぼ登録へ進める |
| UX-071 | TODO | `/map` の通常閲覧を整える | 上部操作、田んぼ選択、現在地、記録、詳細シートの優先度・余白・ラベルを整理 | 地図が常に主役で、操作が画面端で切れない。iPhone実機確認 |
| UX-072 | TODO | 田んぼ詳細から行動へつなぐ | 田んぼ詳細の主CTAを「この田んぼに記録」「地点を追加」「履歴を見る」に限定 | 次の操作を迷わない。情報表示と操作ボタンを混在させない |
| UX-073 | TODO | マップと記録の往復導線を作る | 記録詳細・記録一覧・田んぼ詳細から、該当地点を開く `マップで見る` を統一 | lat/lngまたは田んぼIDがある記録から地図へ戻れる |

---

## Phase 2: 共通UI部品と画面責務を整える

| ID | 状態 | 目的 | 実装範囲 | 完了条件 |
|---|---|---|---|---|
| REF-002 | TODO | 共通UI部品の土台を作る | 画面見出し、戻る導線、主/副ボタン、ボトムシート、空状態、ステータスチップ、確認ダイアログの共通契約を作る | 既存画面へ一括置換しない。2画面だけ試験導入し、再利用可能なAPIを確定する |
| REF-003 | TODO | ナビゲーションと画面責務を固定する | `/map`、`/home`、`/fields`、`/records`、`/calendar`、`/menu` の役割を定義し、重複リンク・重複CTAを洗い出す | 画面ごとに「最初の1操作」が一つに絞られている |
| REF-004 | TODO | 非同期操作の安全策を整える | 田んぼ描き直し保存中の競合を防ぐ。保存要求単位のtokenまたはpending状態で、古い応答が後から画面を上書きしないようにする | 低速通信でも別操作中に古い保存完了で画面が戻らない |

**Phase 2は、大規模な見た目変更をしない。構造だけを整える。**

---

## Phase 3: 記録体験を1分以内へ寄せる

| ID | 状態 | 目的 | 実装範囲 | 完了条件 |
|---|---|---|---|---|
| UX-080 | TODO | 記録開始を統一する | マップ、田んぼ詳細、地点詳細、記録一覧の「記録する」を同じ開始画面・同じ引継ぎ情報へ統一 | 開始場所が違っても、田んぼ・地点・位置情報が失われない |
| UX-081 | TODO | 写真・音声・メモ入力を整理する | 現場入力を写真→音声→最低限の補足へ寄せる。手入力欄は後ろへ置く | 写真または音声だけでも保存でき、保存前確認で内容が分かる |
| UX-082 | TODO | 保存前確認を現場向けにする | 田んぼ、地点、写真、音声、要約、状態、位置を優先順で表示。修正可能箇所を明確化 | 保存前に「何を、どこへ、どの状態で残すか」が一目で分かる |
| UX-083 | TODO | 未対応・対応済みを使える状態にする | 記録・地点・田んぼ詳細で未対応の理由、最終確認日、対応コメントを一貫表示 | 家族が見て、次に誰が何を確認すべきか判断できる |

---

## Phase 4: マップ以外の画面を再設計する

| ID | 状態 | 目的 | 実装範囲 | 完了条件 |
|---|---|---|---|---|
| UX-090 | TODO | `/home` の役割を決める | ホームを「今日見るべきこと」の入口に限定。マップや一覧との重複を削る | マップへ行く理由、未対応を見る理由、最近の記録を見る理由が明確 |
| UX-091 | TODO | 田んぼ一覧を家族共有向けにする | 面積・写真・統計を並べるだけでなく、状態、最終確認、未対応を優先 | 田んぼ比較ではなく、確認漏れ防止に役立つ |
| UX-092 | TODO | 記録一覧・詳細の情報設計を整理 | 検索・絞り込みを最低限に絞り、写真、状態、田んぼ、地点、日時を主役にする | 欲しい履歴に短時間で辿り着ける |
| UX-093 | TODO | カレンダー・メニューを整理 | 初期MVPで必要な予定だけを残す。メニューは設定・家族・使い方・ログアウトを中心にする | 地図・記録導線を邪魔しない |

---

## Phase 5: 使い方と開発ドキュメントを完成させる

| ID | 状態 | 目的 | 実装範囲 | 完了条件 |
|---|---|---|---|---|
| DOC-001 | TODO | アプリ内の使い方を作り直す | `/guide` を「最初にすること」「田んぼ登録」「記録」「未対応確認」「家族共有」の5項目へ再構成 | 初めて使う家族が、3分以内に最初の記録まで進める |
| DOC-002 | TODO | READMEを開発者向けに整理 | セットアップ、環境変数、構成、データ概念、主要フロー、テスト手順、現在の制約を明記 | 新しい実装担当がREADMEだけで起動・確認できる |
| DOC-003 | TODO | 現場用の短い操作カードを作る | iPhoneで見る1画面の簡易ガイドを作る。必要ならPWA内から開ける | 現場で「田んぼ登録」「記録」「対応済み」だけ確認できる |

---

## 3. 後回しにするもの

以下は、上記の導線とUIが固まるまで実装しない。

- REF-001（MapCanvasの責務分割）: UI/UX 改善の具体的な障壁になった時のみ着手。現状は実機確認優先で不要
- AI文字起こし・AI要約の本格運用
- AI画像診断
- センサー連携
- 売上、原価、帳票、補助金
- 全国向けSaaS機能
- 複数グループの本格権限設計
- soft-delete
- 高度な分析ダッシュボード

---

## 4. 次に開始するPR

### PR候補: UX-071（`/map` 通常閲覧の改善）

目的: ユーザーの実機確認（U-011 ほか）で判明した `/map` の導線・密度課題を修正する。

- `/map` に関する実機課題が 1 件でも判明すれば着手できる
- 実機確認で見つかった課題を1PRにまとめる（UX-071 の範囲内）
- 実装コード以外の変更（docs・tasks）は混ぜない

### このPRの完了条件

- ユーザーの iPhone 実機で確認済み
- 地図が常に主役に見える
- 既存5代表フローが壊れていない

---

## 5. 実装運用

1. このロードマップから1件だけ `IN_PROGRESS` にする。
2. 実装前に、対象画面・対象外・完了条件・実機確認項目をPR本文へ書く。
3. 実装後にVercel Previewで確認する。
4. iPhone固有の操作が含まれる場合は、ユーザー実機確認後にのみマージする。
5. マージ後に `tasks/TASKS.md` の履歴へ証拠を追加し、ロードマップの状態を更新する。
