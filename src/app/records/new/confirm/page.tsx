import ConfirmRecordScreen from "../../../../features/records/ConfirmRecordScreen";

/** 保存前確認画面（実データはクライアント側の下書きから表示） */
export default function ConfirmRecordPage() {
  return <ConfirmRecordScreen />;
}
