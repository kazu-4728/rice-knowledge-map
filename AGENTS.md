# AGENTS.md

## 共通ルール

- 作業開始時に `tasks/TASKS.md` の「現在の実行タスク」を読む。
- 現在タスクの目的、範囲、非対象、受入条件、PR単位は `tasks/TASKS.md` を正とする。
- 他のMarkdownに古い方針や矛盾する記述があっても、現在タスクの記述を優先する。
- 現在タスクにない設計判断を恒久ルールとして追加しない。判断不能な場合は実装前に確認する。
- 実装後は `npx tsc --noEmit`、`npm run lint`、`npm run build` を実行する。
- 秘密情報、APIキー、実環境の値をリポジトリへ書かない。
- 運用中に増減・差し替えされる写真・音声・動画はSupabase Storage（バケット作成はユーザー承認後）またはユーザーのアップロード導線で扱い、リポジトリには置かない。リポジトリにはコード配布に必要な固定の小容量素材（アイコン等）だけを置く。
- 実装コード以外の変更（docs・tasks）は実装PRに混ぜない。ただし、同時期に生じた複数のdocs・tasks変更（ルール追記・タスク更新等）は、それぞれ別PRに分割せず1つのPRにまとめる（2026-08-12オーナー指摘: PRを細かくすると対応負荷がかかる）。
- コミュニケーション、コミットメッセージ、PR、コード、ドキュメントはすべて日本語で記述する。思考過程（thinking）や作業中の記述も、コード識別子やツール・エラーの原文出力など日本語化できないものを除き、可能な限り日本語で行う。
- Supabase migration、RLS、Storage設定、外部サービス設定、本番公開、PRマージはユーザー承認後のみ行う。
- PRは `tasks/TASKS.md` に定義された利用体験または導線の完了単位で作る。小作業ごとに分割しない。

## UIUX変更の標準フロー

PR #102で確立した次の流れを、複数ページにまたがるUIUX変更の標準とする。

1. `tasks/TASKS.md` に目的、対象、非対象、受入条件、PR単位、明確な完了点を記載する。「現在の実行タスク」が未定のまま実装を始めない。
2. 実装前に対象ページをブラウザとコードの両方で確認し、固定知識と変動記録、ページごとの役割、既存機能との重複を整理する。
3. 画像主体のモックを作成して画面表示し、オーナー承認を得る。承認前は実装しない。
4. 承認済みモックと受入条件を基準にブランチを作り、合意した利用体験が完了するまで同じブランチ・同じPRで進める。モックと異なる変更が必要になった場合は、理由を示して再確認する。
5. 実装後は型検査、Lint、build、該当導線のE2Eを実行する。UIUX変更ではスマホ幅を含むブラウザ操作確認とスクリーンショット取得を行い、オーナーが画面を確認できるよう表示する。
6. Draft PRでCIとVercel Previewを確認する。オーナーの指示でReady for reviewにした後、Cloud Codexを含むレビューを監視する。
7. 意図が明確で既存方針と矛盾しない指摘は即時修正し、検証、commit、pushを行う。各指摘へ日本語で対応内容と検証結果を返信し、解決済みならResolveする。
8. 曖昧な指摘、仕様変更、互いに矛盾する指摘、Supabase・RLS・Storage・認証・本番設定・有料外部サービスに関わる指摘は勝手に変更せず、判断材料をオーナーへ報告する。
9. 同じエラーが3回発生した場合は作業を中断し、原因、試した内容、元に戻せる状態かを報告する。
10. 未解決レビューがなく、CIとVercelが成功し、オーナーからマージ指示を得た場合だけマージする。マージ後の `tasks`・ルール更新は、実装PRへ混ぜず文書変更としてまとめる。
